public int countEvens(int[] nums) {
  
  int antalLigeTal = 0;
  
  for(int i = 0; i < nums.length; i++){
   int tal = nums[i];
    boolean tjekLige = (tal%2 == 0);
    
    if(tjekLige){antalLigeTal++;} 
  }
  
  return antalLigeTal;
  
}
